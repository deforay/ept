<?php
// Zend_Debug::dump($this->result);die;
$result = $this->result['result'];
$status = false;
foreach($result as $row){
    if($row != null){
        $status = true;
    }    
}
if (isset($this->result) && count($this->result) > 0 && $status) {
?>
    <script>
        Highcharts.chart('<?php echo $this->render;?>', {
            chart: {
                type: 'spline'
            },
            title: {
                text: ''
            },
            subtitle: {
                text: ''
            },
            xAxis: {
                //categories: ["21 Mar", "22 Mar", "23 Mar", "24 Mar", "25 Mar", "26 Mar", "27 Mar"]
                categories: [<?php 
                    if(count($this->result['monthRange']) > 0){
                        foreach($this->result['monthRange'] as $monthYr){
                            echo "'".$monthYr."',";
                        }
                    }
                ?>]
            },
            yAxis: {
                min:0,
                max:100,
                title: {
                    text: 'Percentage %'
                }

            },
            tooltip: {
                crosshairs: true,
                shared: true
            },
            plotOptions: {
                series: {
                    lineWidth: 3
                },
                spline: {
                    marker: {
                        radius: 4,
                        lineColor: '#666666',
                        lineWidth: 1
                    }
                }
            },

            credits: {
                enabled: false
            },

            series: [
                {
                gridLineWidth: 0,
                minorGridLineWidth: 0,
                name: "Shipment Score",
                colorByPoint: true,
                data: [
                <?php 
                    if(count($result) > 0){
                        foreach($result as $row){
                            if($row != null){
                                foreach($row as $val){
                                    echo '
                                    {
                                        name: "'.$val['shipment_code'].'",
                                        y:'.number_format(($val['shipment_score'] + $val['documentation_score']),2).'
                                    },
                                    ';
                                }
                            } 
                        }
                    }

                ?>]
                }
            ]
        });
    </script>
<?php } else {
    echo '<h3 class="text-center">No Shipment Details Available</h3>';
} ?>