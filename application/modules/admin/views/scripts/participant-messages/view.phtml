<?php
$result = $this->result;
?>
<div class="well">
    <form name="addDistributionForm" id="addDistributionForm" method="post" action="<?php echo $this->url(array("module" => "admin", "controller" => "distributions", "action" => "edit"), null, true) ?>" class="form-horizontal bs-example" role="form">
        <fieldset>
            <legend><?= $this->translate->_("View Participant Message"); ?></legend>

            <div class="form-group">
                <label class="col-lg-3 control-label"><?= $this->translate->_("Subject"); ?></label>

                <div class="col-lg-6">
                    <input type="text" id="subject" name="subject" size="50" class="isRequired form-control" readonly value="<?php echo $this->result['subject']; ?>" />
                </div>
            </div>

            <div class="form-group">
                <label class="col-lg-3 control-label"><?= $this->translate->_("Message"); ?></label>

                <div class="col-lg-6">
                    <textarea name="message" readonly class="form-control" id="message"><?php echo $this->result['message']; ?></textarea>
                </div>
            </div>

            <div class="form-group">
                <label class="col-lg-3 control-label"><?= $this->translate->_("Attacment"); ?></label>

                <div class="col-lg-6">
                    <?php
                    if (isset($result['attached_file']) && trim($result['attached_file']) != '' && file_exists(realpath(UPLOAD_PATH) . DIRECTORY_SEPARATOR . 'mail-attachments' . DIRECTORY_SEPARATOR . $result['attached_file'])) {
                    ?>
                        <img src="/uploads/mail-attachments/<?php echo $result['attached_file']; ?>" style="width: 200px; height: 200px;" alt="No image" />
                    <?php } else { ?>
                        <img src="/images/loading.gif" alt="NO file attaced" />
                    <?php } ?>
                </div>
            </div>

            </table>
            <br>

            <div id="respond" style="margin: 0px auto 0px auto; text-align: center;" align="center">
                <input class="btn btn-danger" type="button" onclick="window.location.href='/admin/participant-messages';" tabindex="8" value="<?= $this->translate->_("Back"); ?>" />
            </div>
        </fieldset>
    </form>
</div>