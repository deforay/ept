<?php
/**
 * ePT admin dashboard — round status
 * Rebuilt per "ePT admin dashboard: round status" spec, 12 August 2026.
 *
 * Removed (Task 1):
 *   - the three dark status cards (New PT Surveys / Ready to Ship / Shipped)
 *   - "Active Participants enrolled per PT Scheme" chart + its ajax query
 *   - the email queue health card
 *   - ASSUMPTION: also removed "List of all PT Surveys" (participant-pt-scheme)
 *     and the hidden "Number of PT Survey Participants" panel. Neither was named
 *     in the spec's removal list, but both were chart plumbing for the same
 *     reporting surface the new round table replaces, and neither appears in
 *     the spec's mockups. Flag this to the team — easy to restore if wrong,
 *     see PATCH_NOTES.md.
 *
 * Kept: ePT Overview panel, stale re-evaluation banner, pending-participants nudge.
 */
?>
<style>
	/* Task 2 — summary strip */
	.summary-strip {
		display: flex;
		gap: 20px;
		margin-bottom: 20px;
	}

	.summary-strip .stat {
		flex: 1;
		background: #fff;
		border: 1px solid #e3e3e3;
		border-radius: 4px;
		padding: 14px 18px;
		text-align: center;
	}

	.summary-strip .stat .stat-value {
		font-size: 28px;
		font-weight: 600;
		color: #2c3e50;
		line-height: 1.1;
	}

	.summary-strip .stat .stat-label {
		font-size: 13px;
		color: #777;
		margin-top: 4px;
	}

	/* Task 3 — round table */
	.data-list {
		width: 100%;
		border-collapse: collapse;
		font-size: 13px;
	}

	.data-list th {
		text-align: left;
		font-weight: 600;
		color: #555;
		padding: 8px 10px;
		border-bottom: 2px solid #e3e3e3;
		text-transform: uppercase;
		font-size: 11px;
		letter-spacing: .03em;
	}

	.data-list td {
		padding: 10px;
		border-bottom: 1px solid #eee;
		vertical-align: middle;
	}

	.data-list tr.row-amber {
		background: #fff8e6;
	}

	.data-list tr.row-red {
		background: #fdeeee;
	}

	.data-list .round-code {
		font-weight: 600;
		color: #2b6cb0;
	}

	.data-list .round-scheme {
		display: block;
		color: #888;
		font-size: 12px;
	}

	.data-list .zero {
		color: #bbb;
	}

	.data-list .chip-closed {
		display: inline-block;
		background: #c0392b;
		color: #fff;
		font-size: 11px;
		padding: 1px 7px;
		border-radius: 10px;
		margin-left: 6px;
	}

	.responded-bar-wrap {
		display: inline-block;
		width: 60px;
		height: 6px;
		background: #eee;
		border-radius: 3px;
		vertical-align: middle;
		margin-left: 8px;
		overflow: hidden;
	}

	.responded-bar-fill {
		height: 100%;
		background: #2b6cb0;
	}

	/* Task 4 — between-rounds cards */
	.between-rounds-header {
		color: #777;
		font-size: 13px;
		margin-bottom: 12px;
	}

	.round-card {
		background: #fff;
		border: 1px solid #e3e3e3;
		border-radius: 4px;
		padding: 16px 18px;
		margin-bottom: 12px;
	}

	.round-card .round-card-title {
		font-weight: 600;
		color: #2c3e50;
		margin-bottom: 2px;
	}

	.round-card .round-card-sub {
		color: #888;
		font-size: 12px;
		margin-bottom: 12px;
	}

	.round-card .figure-grid {
		display: flex;
		flex-wrap: wrap;
		gap: 22px;
	}

	.round-card .figure {
		min-width: 150px;
	}

	.round-card .figure-label {
		font-size: 11px;
		text-transform: uppercase;
		letter-spacing: .03em;
		color: #999;
		margin-bottom: 2px;
	}

	.round-card .figure-value {
		font-size: 13px;
		color: #333;
	}

	.round-card .trend-up {
		color: #2e9e5b;
	}

	.round-card .trend-down {
		color: #c0392b;
	}
</style>

<?php echo $this->render('partials/re-evaluate-stale-banner.phtml'); ?>

<div class="row">
	<div class="col-lg-12">
		<ol class="breadcrumb" style="margin-bottom:10px;">
			<li class="active" style="font-size:20px;color:#555"><i class="icon icon-dashboard"></i>
				<?= $this->translate->_("Dashboard"); ?></li>
		</ol>
	</div>
</div>

<?php if (count($this->pendingParticipants) > 0): ?>
	<div class="row" style="margin-bottom:10px;">
		<div class="col-lg-12" style="text-align:center;"><a href="/admin/participants/pending"
				style="color:red;font-size:17px;text-decoration:underline;"><?php echo count($this->pendingParticipants); ?>
				<?= $this->translate->_("participant"); ?>
				<?php echo (count($this->pendingParticipants) > 1) ? ' s have ' : ' has '; ?>
				<?= $this->translate->_("registered but not yet approved to use ePT. Please click here to approve."); ?></a>
		</div>
	</div>
<?php endif; ?>

<!-- Task 2: summary strip -->
<div class="row">
	<div class="col-lg-12">
		<div class="summary-strip">
			<div class="stat">
				<div class="stat-value"><?php echo (int) $this->summaryCounts['activeParticipants']; ?></div>
				<div class="stat-label"><?= $this->translate->_("Active participants"); ?></div>
			</div>
			<div class="stat">
				<div class="stat-value"><?php echo (int) $this->summaryCounts['activeSchemes']; ?></div>
				<div class="stat-label"><?= $this->translate->_("Active schemes"); ?></div>
			</div>
			<div class="stat">
				<div class="stat-value"><?php echo (int) $this->summaryCounts['roundsCompletedThisYear']; ?></div>
				<div class="stat-label"><?= $this->translate->_("Rounds completed in " . date('Y')); ?></div>
			</div>
		</div>
	</div>
</div>

<?php if ($this->showRoundTable): ?>
	<!-- Task 3: round table -->
	<div class="row">
		<div class="col-lg-12">
			<div class="panel panel-primary">
				<div class="panel-heading">
					<h3 class="panel-title">
						<i class="icon-bar-chart"></i>&nbsp;&nbsp;
						<?php echo (int) $this->openRoundsCount; ?>
						<?= $this->translate->_("round"); ?><?php echo $this->openRoundsCount === 1 ? '' : 's'; ?>
						<?= $this->translate->_("open for response"); ?>
					</h3>
				</div>
				<div class="panel-body">
					<table class="data-list">
						<thead>
							<tr>
								<th><?= $this->translate->_("Round"); ?></th>
								<th><?= $this->translate->_("Deadline"); ?></th>
								<th><?= $this->translate->_("Responded"); ?></th>
								<th><?= $this->translate->_("Outstanding"); ?></th>
								<th><?= $this->translate->_("Late"); ?></th>
								<th><?= $this->translate->_("Unable to test"); ?></th>
							</tr>
						</thead>
						<tbody>
							<?php foreach ($this->openRounds as $round): ?>
								<tr class="<?php echo $round['rowClass'] ? 'row-' . $round['rowClass'] : ''; ?>">
									<td>
										<a class="round-code"
											href="<?php echo $this->url(['module' => 'admin', 'controller' => 'shipment', 'action' => 'view', 'id' => $round['shipmentId']], 'default', true); ?>">
											<?php echo $this->escape($round['code']); ?>
										</a>
										<span class="round-scheme"><?php echo $this->escape($round['schemeName']); ?></span>
									</td>
									<td>
										<span title="<?php echo $this->escape($round['deadlineDate']); ?>">
											<?php echo $this->escape($round['deadlineLabel']); ?>
										</span>
										<?php if ($round['isClosed']): ?>
											<span class="chip-closed"><?= $this->translate->_("Closed"); ?></span>
										<?php endif; ?>
									</td>
									<td>
										<?php echo (int) $round['responded']; ?> <?= $this->translate->_("of"); ?>
										<?php echo (int) $round['total']; ?>
										<span class="responded-bar-wrap">
											<span class="responded-bar-fill"
												style="width:<?php echo $round['total'] > 0 ? round(($round['responded'] / $round['total']) * 100) : 0; ?>%;"></span>
										</span>
									</td>
									<td>
										<?php if ($round['outstanding'] > 0): ?>
											<a
												href="<?php echo $this->url(['module' => 'admin', 'controller' => 'shipment', 'action' => 'participants', 'id' => $round['shipmentId'], 'status' => 'outstanding'], 'default', true); ?>">
												<?php echo (int) $round['outstanding']; ?>
											</a>
										<?php else: ?>
											<span class="zero">0</span>
										<?php endif; ?>
									</td>
									<td>
										<?php if ($round['late'] > 0): ?>
											<?php echo (int) $round['late']; ?>
										<?php else: ?>
											<span class="zero">0</span>
										<?php endif; ?>
									</td>
									<td>
										<?php if ($round['unableToTest'] > 0): ?>
											<?php echo (int) $round['unableToTest']; ?>
										<?php else: ?>
											<span class="zero">0</span>
										<?php endif; ?>
									</td>
								</tr>
							<?php endforeach; ?>
						</tbody>
					</table>
				</div>
			</div>
		</div>
	</div>
<?php else: ?>
	<!-- Task 4: between-rounds card -->
	<div class="row">
		<div class="col-lg-12">
			<?php if (!empty($this->betweenRounds)): ?>
				<div class="between-rounds-header">
					<?= $this->translate->_("Last round finalized"); ?>
					<?php echo $this->escape($this->betweenRounds[0]['finalizedDateShort']); ?>
				</div>
				<?php foreach ($this->betweenRounds as $card): ?>
					<div class="round-card">
						<div class="round-card-title"><?php echo $this->escape($card['schemeName']); ?> ·
							<?php echo $this->escape($card['roundCode']); ?>
						</div>
						<div class="round-card-sub">
							<?= $this->translate->_("Finalized"); ?> <?php echo $this->escape($card['finalizedDateFull']); ?>
						</div>
						<div class="figure-grid">
							<div class="figure">
								<div class="figure-label"><?= $this->translate->_("Participation"); ?></div>
								<div class="figure-value">
									<?php echo (int) $card['tookPart']; ?> <?= $this->translate->_("took part"); ?>,
									<?php echo (int) $card['noResponse']; ?> <?= $this->translate->_("no response"); ?>,
									<?php echo (int) $card['unableToTest']; ?> <?= $this->translate->_("unable"); ?><br>
									<?= $this->translate->_("Out of enrolled"); ?> <?php echo (int) $card['enrolledCount']; ?>
								</div>
							</div>
							<div class="figure">
								<div class="figure-label"><?= $this->translate->_("Results"); ?></div>
								<div class="figure-value">
									<?php echo (int) $card['pass']; ?> <?= $this->translate->_("pass"); ?>,
									<?php echo (int) $card['fail']; ?> <?= $this->translate->_("fail"); ?>,
									<?php echo (int) $card['excluded']; ?> <?= $this->translate->_("excluded"); ?>
									<?php if ($card['notEvaluated'] > 0): ?>
										, <?php echo (int) $card['notEvaluated']; ?> <?= $this->translate->_("not evaluated"); ?>
									<?php endif; ?>
								</div>
							</div>
							<div class="figure">
								<div class="figure-label"><?= $this->translate->_("Average score"); ?></div>
								<div class="figure-value">
									<?php echo $this->escape($card['averageScore']); ?>
									<?php if ($card['scoreDelta'] !== null): ?>
										<span class="<?php echo $card['scoreDelta'] >= 0 ? 'trend-up' : 'trend-down'; ?>">
											<?php echo $card['scoreDelta'] >= 0 ? '&uarr;' : '&darr;'; ?>
											<?php echo abs($card['scoreDelta']); ?>
										</span>
									<?php endif; ?>
								</div>
							</div>
							<div class="figure">
								<div class="figure-label"><?= $this->translate->_("Repeat failers"); ?></div>
								<div class="figure-value">
									<?php if ($card['repeatFailerCount'] > 0): ?>
										<a
											href="<?php echo $this->url(['module' => 'admin', 'controller' => 'shipment', 'action' => 'participants', 'id' => $card['shipmentId'], 'status' => 'repeat-fail'], 'default', true); ?>">
											<?php echo (int) $card['repeatFailerCount']; ?> <?= $this->translate->_("repeat failers"); ?>
										</a>
									<?php else: ?>
										<span class="zero">0 <?= $this->translate->_("repeat failers"); ?></span>
									<?php endif; ?>
								</div>
							</div>
							<div class="figure">
								<div class="figure-label"><?= $this->translate->_("Never responded"); ?></div>
								<div class="figure-value">
									<?php if ($card['neverRespondedCount'] > 0): ?>
										<a
											href="<?php echo $this->url(['module' => 'admin', 'controller' => 'shipment', 'action' => 'participants', 'id' => $card['shipmentId'], 'status' => 'never-responded'], 'default', true); ?>">
											<?php echo (int) $card['neverRespondedCount']; ?> <?= $this->translate->_("labs never responded"); ?>
										</a>
									<?php else: ?>
										<span class="zero">0 <?= $this->translate->_("labs never responded"); ?></span>
									<?php endif; ?>
								</div>
							</div>
							<div class="figure">
								<div class="figure-label"><?= $this->translate->_("Report uptake"); ?></div>
								<div class="figure-value">
									<?php echo (int) $card['reportOpenedCount']; ?> <?= $this->translate->_("of"); ?>
									<?php echo (int) $card['tookPart']; ?>
								</div>
							</div>
						</div>
					</div>
				<?php endforeach; ?>
			<?php else: ?>
				<div class="text-center" style="color:#999;padding:30px;">
					<?= $this->translate->_("No finalized rounds yet."); ?>
				</div>
			<?php endif; ?>
		</div>
	</div>
<?php endif; ?>

<div class="row">
	<div class="col-lg-12">
		<div class="panel panel-default" id="ept-overview-panel">
			<div class="panel-heading" style="cursor:pointer;" id="ept-overview-toggle" role="button"
				aria-expanded="false" aria-controls="ept-overview-body">
				<h3 class="panel-title">
					<i class="icon-bar-chart"></i>&nbsp;&nbsp;<?= $this->translate->_("ePT Overview"); ?>
					<i class="icon-chevron-down pull-right" id="ept-overview-chevron"></i>
				</h3>
			</div>
			<div class="panel-body" id="ept-overview-body" style="display:none;">
				<div id="ept-overview-content" class="text-center" style="padding:20px;color:#888;">
					<i class="icon-spinner icon-spin"></i>&nbsp;<?= $this->translate->_("Loading..."); ?>
				</div>
			</div>
		</div>
	</div>
</div>
<br />

<script>
	$(document).ready(function () {
		var loaded = false;
		$('#ept-overview-toggle').on('click', function () {
			var $body = $('#ept-overview-body');
			var $chev = $('#ept-overview-chevron');
			var isOpen = $body.is(':visible');
			if (isOpen) {
				$body.slideUp(200);
				$chev.removeClass('icon-chevron-up').addClass('icon-chevron-down');
				$(this).attr('aria-expanded', 'false');
				return;
			}
			$body.slideDown(200);
			$chev.removeClass('icon-chevron-down').addClass('icon-chevron-up');
			$(this).attr('aria-expanded', 'true');
			if (!loaded) {
				$.get("<?php echo $this->url(['module' => 'admin', 'controller' => 'index', 'action' => 'ept-overview'], 'default', true); ?>", {
					format: "html"
				}, function (data) {
					$('#ept-overview-content').html(data);
					loaded = true;
				}).fail(function () {
					$('#ept-overview-content').text('<?= $this->translate->_("Failed to load overview."); ?>');
				});
			}
		});
	});
</script>