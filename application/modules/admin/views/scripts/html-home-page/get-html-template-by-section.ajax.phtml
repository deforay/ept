<?php
$respons = [];
if(isset($this->result) && !empty($this->result)){
    $result = $this->result[0];
    $respons = array(
        "isHeaderFooter" => ($result['is_header_footer']),
        "content" => html_entity_decode($result['text'])
    );
}
echo json_encode($respons);