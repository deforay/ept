<?php
$ct = $this->certificateTemplates;
?>
<link rel="stylesheet" href="<?php echo $this->baseUrl('css/daterangepicker.css'); ?>" type="text/css" media="all">
<link rel="stylesheet" href="<?php echo $this->baseUrl("css/jquery.dataTables.css"); ?>" type="text/css" media="all">
<h4 style="width:100%;height:50px;border-bottom:1px solid #777;">
    <div style="font-size:22.5px;line-height:36px;color:#333;float:left;">Certificate Template</div>
</h4>
<form enctype="multipart/form-data" name="addCertificateTemplateForm" id="addCertificateTemplateForm" method="post" action="<?php echo $this->url(array("module" => "admin", "controller" => "certificate-template"), 'default', true) ?>" class="form-horizontal bs-example" role="form" onsubmit="validateNow();return false;">
    <table cellpadding=" 0" cellspacing="0" border="0" class="table table-striped table-bordered table-hover" id="auditLogTable">
        <thead>
            <tr>
                <th>Scheme Type</th>
                <th>Participation Certificate</th>
                <th>Excellence Certificate</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($this->schemes as $scheme) {
                if (isset($ct[$scheme['scheme_id']]['participation_certificate']) && $ct[$scheme['scheme_id']]['participation_certificate'] != "") {
                    $pfilePath = DIRECTORY_SEPARATOR . 'scheduled-jobs' . DIRECTORY_SEPARATOR . 'certificate-templates' . DIRECTORY_SEPARATOR . $ct[$scheme['scheme_id']]['participation_certificate'];
                }

                if (isset($ct[$scheme['scheme_id']]['excellence_certificate']) && $ct[$scheme['scheme_id']]['excellence_certificate'] != "") {
                    $efilePath = DIRECTORY_SEPARATOR . 'scheduled-jobs' . DIRECTORY_SEPARATOR . 'certificate-templates' . DIRECTORY_SEPARATOR . $ct[$scheme['scheme_id']]['excellence_certificate'];
                }
                $ctId = (isset($ct[$scheme['scheme_id']]['ct_id']) && $ct[$scheme['scheme_id']]['ct_id'] > 0) ? base64_encode($ct[$scheme['scheme_id']]['ct_id']) : "";
            ?>
                <tr>
                    <th>
                        <?php echo $scheme['scheme_name']; ?>
                        <input type="hidden" name="scheme" id="scheme" value="<?php echo $scheme['scheme_id']; ?>">
                        <input type="hidden" name="ctId" id="ctId" value="<?php echo $ctId; ?>">
                    </th>
                    <td>
                        <?php if (isset($ct[$scheme['scheme_id']]['participation_certificate']) && $ct[$scheme['scheme_id']]['participation_certificate'] != "") { ?>
                            <a href="<?php echo $pfilePath; ?>" class=""><?php echo $ct[$scheme['scheme_id']]['participation_certificate']; ?></a>
                        <?php } else { ?>
                            <input type="file" id="pCertificate" name="pCertificate" class="form-control" title="Please upload the participation certificate" placeholder="Upload the participation certificate" />
                        <?php } ?>
                    </td>
                    <td>
                        <?php if (isset($ct[$scheme['scheme_id']]['excellence_certificate']) && $ct[$scheme['scheme_id']]['excellence_certificate'] != "") { ?>
                            <a href="<?php echo $efilePath; ?>" class=""><?php echo $ct[$scheme['scheme_id']]['excellence_certificate']; ?></a>
                        <?php } else { ?>
                            <input type="file" id="eCertificate" name="eCertificate" class="form-control" title="Please upload the participation certificate" placeholder="Upload the participation certificate" />
                        <?php } ?>
                    </td>
                    <td>
                        <input name="submitbtn" class="btn btn-primary" type="submit" tabindex="7" value="Save" />
                    </td>
                </tr>
            <?php } ?>
        </tbody>
    </table>
</form>
<script type="text/javascript" charset="utf-8">
    $(document).ready(function() {

    });
</script>