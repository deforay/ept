<?php
// Rendered inside the Scheme Config hub frame: this page keeps its own
// controller and URL, but belongs with the rest of the scheme's config.
// No schemeConfigTitle — this page's own <h4> already names it, and the
// sidebar shows which scheme it belongs to.
?>
<?php echo $this->render('partials/scheme-config-open.phtml'); ?>

<div class="config-list">
    <div class="config-list-head">
        <h2><?= $this->translate->_("List of all VL Assays or Platforms"); ?></h2>
        <div class="config-list-actions">
            <a
            href="<?php echo $this->url(array('module' => 'admin', 'controller' => 'vl-assay', 'action' => 'add'), 'default', true); ?>"
            class="btn btn-primary"><i
                class="icon-plus"></i>&nbsp;&nbsp;<?= $this->translate->_("Add New VL Assay/Platform"); ?></a>
        </div>
    </div>
    <div class="config-list-body">
<table cellpadding="0" cellspacing="0" border="0" class="table table-striped table-bordered table-hover"
    id="vlAssayTable">
    <thead>
        <tr>
            <th scope="col" style="width:40%;"><?= $this->translate->_("Name"); ?></th>
            <th scope="col" style="width:40%;"><?= $this->translate->_("Short Name"); ?></th>
            <th scope="col"><?= $this->translate->_("Action"); ?></th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td colspan="3" class="dataTables_empty"><?= $this->translate->_("Loading data from server"); ?></td>
        </tr>
    </tbody>
</table>
    </div><!-- /.config-list-body -->
</div><!-- /.config-list -->

<script type="text/javascript" charset="utf-8">
    /* Table initialisation */
    var oTable = null;
    $(document).ready(function () {
        oTable = $('#vlAssayTable').dataTable({

            "bJQueryUI": false,
            "bAutoWidth": false,
            "bInfo": true,
            "bScrollCollapse": true,
            "sPaginationType": "bootstrap",
            "bRetrieve": true,
            "aoColumns": [{
                "sClass": ""
            },
            {
                "sClass": ""
            },
            {
                "sClass": "center",
                "bSortable": false
            }
            ],
            "aaSorting": [
                [0, "ASC"]
            ],
            "bProcessing": true,
            "bServerSide": true,
            "sAjaxSource": "<?php echo $this->url(array('module' => 'admin', 'controller' => 'vl-assay', 'action' => 'index', 'format' => 'html')); ?>",
            "fnServerData": function (sSource, aoData, fnCallback) {
                $.ajax({
                    "dataType": 'json',
                    "type": "POST",
                    "url": sSource,
                    "data": aoData,
                    "success": fnCallback
                });
            }
        });

    });
</script>
<?php echo $this->render('partials/scheme-config-close.phtml'); ?>
