<?php
//----------------Summary Statistics for this survey  start----------------

// Initialize flags up front to avoid undefined-variable issues driving the chart positioning logic
$performance1 = false;
$performance2 = false;
$performance3 = false;

// Charts are written to temp PNGs and unlinked right after. Report generation runs many
// worker subprocesses over the same shipment in parallel, so a filename keyed only on
// shipment_code collides across workers (JpGraph 25111 on unlink; TCPDF "Unable to get
// the size of the image" when another worker deletes mid-render). Make the path unique
// per participant + worker so the temp files never overlap.
$chartUid = preg_replace('/[^A-Za-z0-9_-]/', '', (string)($result['unique_identifier'] ?? '')) . '_' . getmypid();

if (isset($result['statistics']) && !empty($result['statistics'])) {
    $passpercentage = Pt_Commons_SchemeConfig::get($schemeType . '.passPercentage') ?? Pt_Commons_SchemeConfig::get($schemeType . '.passingScore');
    $score = $passpercentage ?? '100';

    $ppOverview = '<br><table border="1" cellpadding="4" cellspacing="0" style="width:100%;font-size:12px;">';

    $ppOverview .= '<tr style="font-size:12px;font-weight:bold;text-align:center;">';
    $ppOverview .= '<td colspan="5" style="text-align:center;background-color:#dbe4ee;">STATISTICS FOR THIS SURVEY</td>';
    $ppOverview .= '</tr>';

    $ppOverview .= '<tr style="background-color:#dbe4ee;">';
    $ppOverview .= '<td width="20%" style="background-color:#dbe4ee;text-align:center;">Number of Participants that responded</td>';
    $ppOverview .= '<td width="20%" style="background-color:#dbe4ee;text-align:center;">Participants who scored 100%</td>';
    $ppOverview .= '<td width="20%" style="background-color:#dbe4ee;text-align:center;">Participants who scored &gt;= ' . $score . '%</td>';
    $ppOverview .= '<td width="20%" style="background-color:#dbe4ee;text-align:center;">Participants who scored &lt; ' . $score . '%</td>';
    $ppOverview .= '<td width="20%" style="background-color:#dbe4ee;text-align:center;">Providers that did not Respond</td>';
    $ppOverview .= '</tr>';

    $ppOverview .= '<tr>';
    $ppOverview .= '<td width="20%" style="text-align:center;font-weight:normal;">' . $result['statistics']['number_responded'] . '</td>';
    $ppOverview .= '<td width="20%" style="text-align:center;font-weight:normal;">' . $result['statistics']['providersWith100'] . '</td>';
    $ppOverview .= '<td width="20%" style="text-align:center;font-weight:normal;">' . $result['statistics']['providers>80'] . '</td>';
    $ppOverview .= '<td width="20%" style="text-align:center;font-weight:normal;">' . $result['statistics']['providers<80'] . '</td>';
    $ppOverview .= '<td width="20%" style="text-align:center;font-weight:normal;">' . $result['statistics']['number_not_responded'] . '</td>';
    $ppOverview .= '</tr>';

    $ppOverview .= '</table><br>';

    // Keep the whole statistics table on one page. When the corrective-actions table above runs
    // long, this table can start too low and TCPDF splits it mid-table — stranding the blue
    // "STATISTICS FOR THIS SURVEY" caption at the foot of one page and the data on the next.
    // It's a fixed 3-row block (caption + a header row that wraps to ~3 lines + one data row),
    // so estimate its height and break BEFORE writing if it won't fit — same cheap fit-check the
    // charts use below, no TCPDF transaction/object-clone per participant.
    $statsTableHeight = 40; // mm, generous estimate for the 3-row block incl. wrapped header
    if (($pdf->getY() + $statsTableHeight) > ($pdf->getPageHeight() - $pdf->getBreakMargin())) {
        $pdf->AddPage();
    }
    $pdf->writeHTML($ppOverview, true, false, true, false, '');
}

// Track whether this section is starting on a page TCPDF just freshly broke to. If so,
// there is no "current page" content to weigh against — we're already as fresh as it
// gets, so the up-front fits-or-breaks check further below must be skipped. Running it
// anyway was the bug: it re-measured against the small amount of space already eaten by
// the header/margin on the new page, found the group didn't fit THAT tightly, and fired
// a second AddPage() — leaving the page we just broke to sitting completely blank.
$justForcedFreshPage = false;
if (!$correctiveActions) {
    $pdf->AddPage();
    $justForcedFreshPage = true;
}

// Build (but don't write yet) the "Qualitative Evaluation" heading, so the page-break
// decision below can run before it lands on the page — otherwise the heading prints first
// and then gets stranded when the charts break to the next page.
$qualEvalHtml = '<span style="text-align:center;font-weight:bold;font-size:12px;">Qualitative Evaluation</span>';

// All three charts are drawn at the same Graph canvas size (680x200), so lock every
// rendered image to the same width and derive its height from that fixed aspect ratio.
// This keeps our height math and the actual TCPDF-rendered size in sync.
$chartWidthMM  = 200;
$chartHeightMM = ($chartWidthMM / 680) * 200;
$chartSpacing  = 5;

// Build the PT Scheme Manager Comments HTML now; it's written right after the charts and
// relies on TCPDF's native auto page break if it runs past the bottom of the last chart page.
$eodReportText = "End of report";
if ($evalRow['report_type'] == 'finalized') {
    $eodReportText = "End of final report";
}

$commentsHtml = '<span style="font-size:11px;text">More information can be obtained from PT Participant manual: NPHQAP-PTS-MAN-003 or contact PT Scheme manager.<br>
This is the confidential report that summarizes your performance. The National Public Health Quality Assurance Program recommends that the results of this proficiency testing scheme comparison not be used as a sole criterion for judging the performance of any individual testing facility/Laboratory.</span><br>';
$commentsHtml .= '<br><span style="font-size:11px;text-align:center;font-weight:bold;"><u>' . $eodReportText . '</u>.</span>';

// Keep the "Qualitative Evaluation" heading attached to at least the first chart: if we're
// continuing mid-page (corrective actions sit above us) and the heading + one chart won't fit
// in what's left of the page, break first so the heading never strands alone at the bottom.
// Each chart below also fit-checks itself, so the remaining charts pack on and flow naturally.
//
// Heights here are deliberately small constants rather than measured. The old approach wrote
// the heading and comments into a transaction and diffed getY() to size them — but when that
// measurement runs near the bottom of a full page, writeHTML auto-breaks mid-measurement,
// getY() jumps to the top of the next page, and the computed height comes back NEGATIVE. That
// negative fed $neededHeight, tripped the `> 0` guard, so the up-front break never fired and
// the heading was left orphaned at the foot of the page (with each chart then cascading onto
// its own page via the stale-Y bug below). A constant can't page-break, so it can't lie.
$qualEvalHeight = 10; // a single bold line, in mm
if (!$justForcedFreshPage) {
    $firstChartHeight = 0;
    if ((isset($result['performance1']) && !empty($result['performance1']))
        || (isset($result['performance2']) && !empty($result['performance2']))
        || (isset($result['performance3']) && !empty($result['performance3']))
    ) {
        $firstChartHeight = $chartHeightMM + $chartSpacing;
    }
    if (($pdf->getY() + $qualEvalHeight + $firstChartHeight) > ($pdf->getPageHeight() - $pdf->getBreakMargin())) {
        $pdf->AddPage();
    }
}

// Now that the page-break decision is finalized, actually write the heading.
$pdf->writeHTML($qualEvalHtml, true, false, true, false, '');

if (isset($result['performance1']) && !empty($result['performance1'])) {
    $performance1 = true;
    $dataX = [];
    $dataY = [];
    foreach ($result['performance1'] as $key => $val) {
        $dataX[] = ($val['scored'] > 0) ? $val['scored'] : 0;
        $dataY[] = $val['distribution_code'];
    }

    $graph = new Graph(680, 200, 'auto');
    $graph->SetScale("textlin");

    $graph->SetBox(false);
    $graph->SetMargin(80, 30, 10, 50);

    $graph->ygrid->SetFill(false);
    $graph->xaxis->SetTickLabels($dataY);
    $graph->xaxis->SetPos(0.5);

    $graph->yaxis->HideLine(false);
    $graph->yaxis->HideTicks(false, false);
    $graph->yaxis->scale->SetAutoMin(0);
    $graph->yaxis->scale->SetAutoMax(100);

    $b1plot = new BarPlot($dataX);
    $gbplot = new GroupBarPlot(array($b1plot));
    $graph->Add($gbplot);

    $b1plot->value->Show();
    $b1plot->value->SetColor("black", "darkred");
    $b1plot->value->SetFormat("%3d%%");

    $b1plot->SetFillColor('#4F81BD');
    $b1plot->SetColor("white");

    // ---- FIX: constant bar width regardless of number of data points ----
    $numPoints = count($dataY);
    $maxBarWidth = 30; // px, tweak to taste
    $barWidth = ($numPoints > 0) ? min($maxBarWidth, 300 / $numPoints) : $maxBarWidth;
    $b1plot->SetAbsWidth($barWidth);
    // -----------------------------------------------------------------------

    $graph->title->Set("Your Performance for the last 5 surveys");
    $graph->subtitle->Set('.');
    $graph->xaxis->title->Set('PT Surveys');
    $graph->yaxis->title->Set('Percentage of Performance');
    $graph->yaxis->SetPos('min');
    $graph->title->SetFont(FF_DEFAULT, FS_BOLD, 10);
    $graph->xaxis->title->SetFont(FF_FONT0, FS_NORMAL, 12);
    $graph->yaxis->title->SetFont(FF_FONT0, FS_NORMAL, 12);
    $graph->yaxis->SetTitleMargin(40);
    $graph->xaxis->SetTitleMargin(15);
    $graph->xaxis->SetTickSide(SIDE_DOWN);
    $graph->yaxis->SetTickSide(SIDE_LEFT);

    $image_file = $reportsPath . DIRECTORY_SEPARATOR . $result['shipment_code'] . DIRECTORY_SEPARATOR . 'bar_chart_' . $chartUid . '.png';
    $graph->img->SetImgFormat('png');
    $graph->img->SetQuality(100);
    $graph->Stroke($image_file);

    $currentY = $pdf->getY();
    // Break before drawing if this chart won't fit in what's left of the page, and pass the
    // image an explicit height. Otherwise Image() auto-breaks under us and the SetY() below
    // advances from a stale $currentY on the wrong page — which is what pushed every chart
    // onto its own page. With the fit-check + explicit height, Image() never auto-breaks, so
    // $currentY and the page always agree.
    if (($currentY + $chartHeightMM) > ($pdf->getPageHeight() - $pdf->getBreakMargin())) {
        $pdf->AddPage();
        $currentY = $pdf->getY();
    }
    $pdf->Image($image_file, 5, $currentY, $chartWidthMM, $chartHeightMM, '', '', '', false, 300);
    $pdf->SetY($currentY + $chartHeightMM + $chartSpacing);

    if (file_exists($image_file)) {
        unlink($image_file);
    }
}

if (isset($result['performance2']) && !empty($result['performance2'])) {
    $performance2 = true;
    $data1y = $data2y = $label = [];
    foreach ($result['performance2'] as $row) {
        $data1y[] = ($row['passed'] > 0) ? $row['passed'] : null;
        $data2y[] = ($row['failed'] > 0) ? $row['failed'] : null;
        $label[] = $row['distribution_code'];
    }

    $graph = new Graph(680, 200, 'auto');
    $graph->SetScale("textlin");

    $graph->SetBox(false);
    $graph->SetMargin(80, 30, 10, 50);
    $graph->ygrid->SetFill(false);
    $graph->xaxis->SetTickLabels($label);
    $graph->title->SetFont(FF_DEFAULT, FS_BOLD, 10);
    $graph->xaxis->title->SetFont(FF_FONT0, FS_NORMAL, 12);
    $graph->yaxis->title->SetFont(FF_FONT0, FS_NORMAL, 12);
    $graph->xaxis->HideLine(false);
    $graph->yaxis->HideLine(false);
    $graph->xaxis->HideTicks(false, false);
    $graph->yaxis->HideTicks(false, false);

    $b1plot = new BarPlot($data1y);
    $b2plot = new BarPlot($data2y);
    $gbplot = new GroupBarPlot(array($b1plot, $b2plot));
    $graph->Add($gbplot);

    $b1plot->value->Show();
    $b1plot->value->SetColor("black", "darkred");
    $b2plot->value->Show();
    $b2plot->value->SetColor("darkred", "black");
    $b1plot->value->SetFormat("%d");
    $b2plot->value->SetFormat("%d");

    $b1plot->SetFillColor('#4F81BD');
    $b1plot->SetColor("white");

    $b2plot->SetFillColor("red");
    $b2plot->SetColor("white");

    // Let JpGraph's native grouped layout size and place the Passed/Failed pair. The group
    // fills 0.7 of each category slot, split evenly between the two bars, so they sit right
    // next to each other (touching). Forcing a fixed 30px SetAbsWidth here used to leave the
    // bars thin while their positions stayed 0.35-category apart — stranding them far apart.
    $gbplot->SetWidth(0.7);

    $b1plot->SetLegend('Passed');
    $b2plot->SetLegend('Failed');
    $graph->legend->SetShadow('gray@0.4', 5);
    $graph->legend->SetPos(0.5, 0.95, 'center', 'bottom');
    $graph->title->Set("Participants performance for the last 5 surveys");
    $graph->subtitle->Set('.');
    $graph->yaxis->title->Set('Number of Participants');
    $graph->xaxis->title->Set('PT Surveys');
    $graph->yaxis->SetTitleMargin(40);
    $graph->xaxis->SetTitleMargin(15);
    $graph->xaxis->SetTickSide(SIDE_DOWN);
    $graph->yaxis->SetTickSide(SIDE_LEFT);

    $performance_of_surveys = $reportsPath . DIRECTORY_SEPARATOR . $result['shipment_code'] . DIRECTORY_SEPARATOR . 'performance_of_surveys_' . $chartUid . '.png';
    $graph->img->SetImgFormat('png');
    $graph->img->SetQuality(100);
    $graph->Stroke($performance_of_surveys);

    $currentY = $pdf->getY();
    if (($currentY + $chartHeightMM) > ($pdf->getPageHeight() - $pdf->getBreakMargin())) {
        $pdf->AddPage();
        $currentY = $pdf->getY();
    }
    $pdf->Image($performance_of_surveys, 5, $currentY, $chartWidthMM, $chartHeightMM, '', '', '', false, 300);
    $pdf->SetY($currentY + $chartHeightMM + $chartSpacing);

    if (file_exists($performance_of_surveys)) {
        unlink($performance_of_surveys);
    }
}

if (isset($result['performance3']) && !empty($result['performance3'])) {
    $performance3 = true;
    $surveyNo = "";
    $data1y = $data2y = $label = [];
    foreach ($result['performance3'] as $row) {
        $data1y[] = ($row['passed'] > 0) ? $row['passed'] : 0;
        $data2y[] = ($row['failed'] > 0) ? $row['failed'] : 0;
        $label[] = $row['sample_label'];
        $surveyNo = $row['distribution_code'];
    }

    $graph = new Graph(680, 200, 'auto');
    $graph->SetScale("textlin");

    $graph->SetBox(false);
    $graph->SetMargin(80, 30, 10, 50);
    $graph->ygrid->SetFill(false);
    $graph->xaxis->SetTickLabels($label);
    $graph->title->SetFont(FF_DEFAULT, FS_BOLD, 10);
    $graph->xaxis->title->SetFont(FF_FONT0, FS_NORMAL, 12);
    $graph->yaxis->title->SetFont(FF_FONT0, FS_NORMAL, 12);
    $graph->xaxis->HideLine(false);
    $graph->yaxis->HideLine(false);
    $graph->xaxis->HideTicks(false, false);
    $graph->yaxis->HideTicks(false, false);
    $b1plot = new BarPlot($data1y);
    $b2plot = new BarPlot($data2y);
    $gbplot = new GroupBarPlot(array($b1plot, $b2plot));
    $graph->Add($gbplot);

    $b1plot->value->Show();
    $b1plot->value->SetColor("black", "darkred");
    $b2plot->value->Show();
    $b2plot->value->SetColor("darkred", "black");
    $b1plot->value->SetFormat("%d");
    $b2plot->value->SetFormat("%d");

    $b1plot->SetFillColor('#4F81BD');
    $b1plot->SetColor("white");

    $b2plot->SetFillColor("red");
    $b2plot->SetColor("white");

    // Let JpGraph's native grouped layout size and place the Passed/Failed pair. The group
    // fills 0.7 of each category slot, split evenly between the two bars, so they sit right
    // next to each other (touching). Forcing a fixed 30px SetAbsWidth here used to leave the
    // bars thin while their positions stayed 0.35-category apart — stranding them far apart.
    $gbplot->SetWidth(0.7);

    $graph->legend->SetShadow('gray@0.4', 5);
    $graph->legend->SetPos(0.5, 0.2, 'center', 'bottom');
    $graph->title->Set("Participants performance for the current survey panels");
    $graph->subtitle->Set('Survey No: ' . $surveyNo);
    $graph->subsubtitle->Set('.');
    $graph->yaxis->title->Set('Number of Participants');
    $graph->xaxis->title->Set('Samples');
    $graph->yaxis->SetTitleMargin(40);
    $graph->xaxis->SetTitleMargin(15);
    $graph->xaxis->SetTickSide(SIDE_DOWN);
    $graph->yaxis->SetTickSide(SIDE_LEFT);

    $performance_of_current_survey_anels = $reportsPath . DIRECTORY_SEPARATOR . $result['shipment_code'] . DIRECTORY_SEPARATOR . 'performance_of_current_survey_anels_' . $chartUid . '.png';
    $graph->img->SetImgFormat('png');
    $graph->img->SetQuality(100);
    $graph->Stroke($performance_of_current_survey_anels);

    $currentY = $pdf->getY();
    if (($currentY + $chartHeightMM) > ($pdf->getPageHeight() - $pdf->getBreakMargin())) {
        $pdf->AddPage();
        $currentY = $pdf->getY();
    }
    $pdf->Image($performance_of_current_survey_anels, 5, $currentY, $chartWidthMM, $chartHeightMM, '', '', '', false, 300);
    $pdf->SetY($currentY + $chartHeightMM + $chartSpacing);

    if (file_exists($performance_of_current_survey_anels)) {
        unlink($performance_of_current_survey_anels);
    }
}

// PT Scheme Manager Comments — written straight after the charts. TCPDF's native auto page
// break (SetAutoPageBreak) protects this text block if it runs past the bottom, so no manual
// measurement or pre-break is needed here.
$pdf->writeHTML($commentsHtml, true, false, true, false, '');

//----------------Summary Statistics for this survey  Ends----------------